# 설계 (알파제로 방식)

> 틀입니다. 각 항목은 팀이 결정하면서 채우고, 결정한 이유는 [DECISIONS.md](DECISIONS.md)에 남깁니다.

## 전체 구조

```
자가대국 (MCTS + 신경망) -> 데이터(국면, 정책, 결과) -> 학습 -> 새 모델 -> 대전 판정 -> 통과 시 승격 -> 반복
```

## 1. 규칙 엔진 (환경)

- `infra/engine-merged.js`를 환경으로 사용: `generateActions`(가능한 수), `applyAction`(수 적용), `cloneState`(복제)
- TODO: MCTS에서 상태 복제 비용을 어떻게 줄일지

## 2. 국면 인코딩

- 출발점: `infra/nnue/encode.js`(입력 5509개)
- TODO: 정책/가치망에 맞는 입력 표현

## 3. 정책 출력 (가장 큰 설계 과제)

- 문제: 카드와 특수 기물이 수백 종이라 "수 하나"를 고정 크기 출력으로 표현하기 어렵다
- 선택지 TODO:
  - A. 수 목록에 대한 점수 방식(수마다 특징을 뽑아 점수)
  - B. 고정 크기 행동 공간(출발 칸, 도착 칸, 카드 등을 분해)
  - C. 그 밖의 방식

## 4. 신경망

- TODO: 정책 헤드와 가치 헤드, 구조, 크기(CPU와 자바스크립트 제약 고려)

## 5. MCTS

- TODO: 탐색 횟수, 탐험 계수, 노이즈, 온도

## 6. 자가대국과 학습

- 출발점: `infra/selfplay-worker-merged.js`, `infra/nnue/train.js`
- TODO: 기록할 것(국면, 방문 수 분포, 승패), 학습 손실, 데이터 섞기

## 7. 평가와 승격

- 도구: `infra/tools/lab/lab.js`, `.github/workflows/match.yml`
- 규칙: 95% 신뢰구간 하한이 50%를 넘고 독립 재실행에서도 같은 방향일 때만 승격
